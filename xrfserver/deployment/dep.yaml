apiVersion: v1
kind: Service
metadata:
  name: xrfsapi
  namespace: xrf
spec:
  selector:
    app: xrfsapi
  type: ClusterIP
  ports:
    - name: http
      port: 9090
      targetPort: 9090
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: xrfs
  namespace: xrf
  labels:
    app: xrfs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: xrfs
  template:
    metadata:
      annotations:
              #config.linkerd.io/enable-debug-sidecar: "true"
              #linkerd.io/inject: enabled
      labels:
        app: xrfs
    spec:
      containers:
      - name: xrfs
        image: tolgaomeratalay/xrfserver:4.1
        imagePullPolicy: IfNotPresent
        env:
        - name: CLIENT_COUNT
          value: "20"
        - name: THREAD_COUNT
          value: "20"
        resources:
          requests:
            cpu: 18
            memory: "32560M"
          limits:
            cpu: 18
            memory: "32560M"
        ports:
        - containerPort: 9090
          name: http1
          protocol: TCP
